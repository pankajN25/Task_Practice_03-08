import './task1.css';

// TASK 1 — Student Feedback Form
// This is your entry point. Build everything from here.
//
// Read TASKS.md in the project root for the full requirements.
//
// Suggested components to create in this folder:
//   FeedbackForm.js   — the form + validation
//   FeedbackList.js   — maps over the submitted entries
//   FeedbackCard.js   — one entry + its Delete button
//
// The feedback list state should live HERE in Task1App, then be passed
// down to the children as props.

function Task1App() {
  return (
    <div className="task-placeholder">
      <h2>Task 1 — Student Feedback Form</h2>
      <p>
        Start building in <code>src/task1/Task1App.js</code>
      </p>
      <p>Requirements are in TASKS.md at the project root.</p>
    </div>
  );
}

export default Task1App;
